if first pipe then FPIPE (reads normaly and outputs to fd[1])

if not first and not last pipe then PIPE (reads from fd[0]
                                     and outputs to fd[1])

if last pipe them EPIPE (reads from fd[0]
                            and outputs to normaly (stdout))






		pipe(g_fd);
		save_out = dup(fileno(stdout));
		dup2(g_fd[1], 1);
		printf ("%s\n", pwd);
		close(g_fd[1]);
		dup2(save_out, fileno(stdout));